import { saveLocalStorageWatch } from "./js/localStorage";
const button = document.querySelector('.btn3');
const button1 = document.querySelector('.btn2');

button.addEventListener('click', saveLocalStorageWatch);
button1.addEventListener('click', saveLocalStorageWatch);